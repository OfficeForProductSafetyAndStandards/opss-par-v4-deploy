# WARNING: This is an autogenerated file, not for manual editing.
# Generated by platform-helper 13.2.0 / 2025-03-18 14:36:47
Metadata:
  cfn-lint:
    config:
      ignore_checks:
        - W2001 # Parameter not used

Parameters:
  # Copilot required Parameters...
  App:
    Type: String
    Description: Your application's name.
  Env:
    Type: String
    Description: The environment name your service, job, or workflow is being deployed to.
  Name:
    Type: String
    Description: The name of the service, job, or workflow being deployed.

Mappings:
  bucketEnvironmentConfigMap:
    # Create an entry for each environment

    prod:
      BucketName: 'par-prod-s3'
      KmsKeyArn: 'arn:aws:kms:eu-west-2:597088022393:key/5b9199a8-1418-4f1e-8655-44a924afe9b2'

    dev:
      BucketName: 'par-dev-s3'
      KmsKeyArn: 'arn:aws:kms:eu-west-2:970547347857:key/374fb1cc-82af-4118-bd63-44b40f249923'


Resources:
  bucketS3AccessPolicy:
    Metadata:
      'aws:copilot:description': 'An IAM ManagedPolicy for your service to access the bucket'
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Grants Read access to the S3 bucket.
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Sid: KMSDecryptAndGenerate
            Effect: Allow
            Action:
              - kms:Decrypt
              - kms:GenerateDataKey
            Resource: !FindInMap [bucketEnvironmentConfigMap, !Ref Env, KmsKeyArn]
          - Sid: S3ObjectActions
            Effect: Allow
            Action:
              - s3:*Object

            Resource: !Sub
              - "arn:aws:s3:::${bucket_name}/*"
              - bucket_name: !FindInMap [bucketEnvironmentConfigMap, !Ref Env, BucketName]
          - Sid: S3ListAction
            Effect: Allow
            Action: s3:ListBucket
            Resource: !Sub
              - "arn:aws:s3:::${bucket_name}"
              - bucket_name: !FindInMap [bucketEnvironmentConfigMap, !Ref Env, BucketName]
Outputs:
  bucketAccessPolicy:
    Description: "The IAM::ManagedPolicy to attach to the task role"
    Value: !Ref bucketS3AccessPolicy
